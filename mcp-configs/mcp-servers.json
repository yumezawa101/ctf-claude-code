{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-playwright"],
      "description": "CTF: ブラウザ自動化 (Web問題のDOM操作、スクリーンショット、フォーム自動入力)"
    },
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-puppeteer"],
      "description": "CTF: ブラウザ自動化 (Playwright代替)"
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-fetch"],
      "description": "CTF: HTTP リクエスト (curl代替、ヘッダー詳細確認)"
    }
  },
  "_comments": {
    "usage": "~/.claude.json の mcpServers セクションにコピー",
    "ctf_note": "Kali Linux ではほとんどのツールがCLIで利用可能。MCP は補助的に使用"
  }
}
